Handwritten Digit Recognition App

An end-to-end AI project that recognizes handwritten digits (0–9) using a CNN trained on the MNIST dataset.

##  Tech Stack
- TensorFlow & Keras for model building
- Streamlit for interactive web app
- NumPy, Pillow for preprocessing

##  Features
- Upload an image of a digit
- Real-time AI prediction
- Simple UI, deployable via Streamlit Cloud

##  Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py